/** @type {import('next').NextConfig} */

// ********************************************************************************
const nextConfig = {
  reactStrictMode: false/*prevent effects from running twice*/,
};

// == Export ======================================================================
module.exports = nextConfig;
